# PBI-08-01 Implement initial multi-agent forecast orchestration slice

## Description
Implement the first production-shaped slice of the multi-agent prediction engine using the current Supabase edge-function architecture and existing analysis UI.

## Status History
- 2026-03-20T00:00:00Z | user_scope_confirmed | Proposed -> Agreed | User requested the codebase review, detailed architecture plan, and initial implementation slice for a multi-agent prediction engine. | User
- 2026-03-20T00:00:00Z | start_work | Agreed -> InProgress | Started blueprint documentation and initial orchestration implementation. | AI_Agent

## Scope
- Document the codebase audit findings and target architecture in the docs folder.
- Add a new `multi-agent-forecast` edge function.
- Integrate the new engine into `useStrategyAnalysis`.
- Surface the result in `StrategyConsole`.
- Avoid new DB migrations because `docs/delivery/PBI-06/drift_note.md` records active schema drift and a migration freeze.

## Files
- `docs/delivery/backlog.md`
- `docs/delivery/PBI-08/prd.md`
- `docs/delivery/PBI-08/tasks.md`
- `docs/delivery/PBI-08/PBI-08-01.md`
- `docs/geopolical_predictions.md`
- `docs/multi_agent_prediction_engine_blueprint.md`
- `src/lib/supabase.ts`
- `src/types/strategic-analysis.ts`
- `src/hooks/useStrategyAnalysis.ts`
- `src/components/StrategyConsole.tsx`
- `supabase/functions/multi-agent-forecast/index.ts`

## Acceptance Criteria
- A new edge function returns structured multi-agent forecast output for a scenario plus evidence bundle.
- The analysis hook calls the new engine as part of the enhancement chain.
- The live console analysis UI displays the structured output.
- Documentation clearly distinguishes implemented reality, current gaps, target architecture, checkpoints, and adversarial analysis.
- No new database migrations are introduced in this slice.

## Test Plan
- Build the frontend.
- Confirm the new edge function contract is syntactically valid and integrated into `ENDPOINTS`.
- Manually verify that the console can render the new multi-agent forecast block when data is present.

## Verification
- `pnpm exec tsc --noEmit -p tsconfig.app.json` currently fails due repo-wide dependency/type-resolution issues already affecting many existing files (`react`, `react/jsx-runtime`, `lucide-react`, `@supabase/supabase-js`, Vite env typing), so full workspace type validation is blocked.
- The new slice is wired into the live console route, documented in `docs`, and kept migration-free as planned.
